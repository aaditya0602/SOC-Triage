#!/bin/sh
# Wazuh integration wrapper — delegates to the python script next to it.
WPYTHON="/var/ossec/framework/python/bin/python3"
SCRIPT_DIR=$(dirname "$0")
exec "$WPYTHON" "$SCRIPT_DIR/custom-soar.py" "$@"
